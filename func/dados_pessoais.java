package func;
import config.classes.Sessao;;

public class dados_pessoais {
    
    public static String innit (Sessao session){

        String opc;
        if (session.getEstado_login() == true){

        }
        else{}
    }
}
